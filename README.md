# california-housing-regression
Predicting California median house values using data preprocessing and regression models (Linear Regression, Decision Tree, Random Forest, XGBoost), with model comparison via MAE, RMSE, and R².
